<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ziizii.hallmanagementsystem">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">
        <activity
            android:name=".EmptyHallsActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".EmptyHallsListActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MenuActivity"
            android:label="@string/title_activity_menu"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".StudentMenuActivity" />
        <activity android:name=".InstructorMenuActivity" />
        <activity android:name=".CourseFieldActivity" />
        <activity android:name=".ReserveSlotActivity" />
        <activity android:name=".CourseTimesListActivity" />
        <activity android:name=".CourseDescriptionActivity" />
        <activity android:name=".GpaViewActivity" />
        <activity android:name=".RegisteredCoursesActivity" />
        <activity android:name=".TaughtCoursesActivity" />
        <activity android:name=".StudentListActivity"></activity>
    </application>

</manifest>